# Core
streamlit>=1.40
requests>=2.31
pandas>=2.0

# Tool 2 transcripts (optional but recommended — without it, use the manual
# paste box in "Analyse a Specific Video"). Sometimes IP-blocked on cloud hosts;
# usually fine from a home machine.
youtube-transcript-api>=0.6.2

# storage.py uses sqlite3, which ships with Python — no install needed.
