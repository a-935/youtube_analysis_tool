# YouTube Niche Research — third-party dependencies
# (everything else used — os, re, json, statistics, collections, datetime, html — is
#  in the Python standard library and needs no install.)
#
# For exact pinned versions matching YOUR machine, run in the PyCharm terminal:
#     pip freeze > requirements.txt
# and commit the result. The unpinned list below is enough to get running.

streamlit>=1.30
requests>=2.31
pandas>=2.0
